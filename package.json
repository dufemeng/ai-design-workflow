{
  "name": "ai-design-workflow",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AI Design Workflow System — 给 Claude Code / Codex agent 调用的设计到代码工作流底座，提供 Flow Ledger、gap 验证、Impeccable detect 和 handoff/import 协议。",
  "bin": {
    "adw": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && for f in scripts/verify-*.mjs; do echo \"== $f ==\"; node \"$f\" || exit 1; done",
    "adw": "node dist/cli.js"
  },
  "dependencies": {
    "playwright": "^1.60.0",
    "yaml": "^2.9.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
